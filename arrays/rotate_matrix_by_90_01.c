/*
 * C program rotate the matrix bu 90degrees anti-lockwise direction using for loop
 * */

#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>

void rotateMatrix(int *arr, int n) {
    int temp[n][n];




}

int main() {
    
    int arr[3][3] = {{1,2,3},{4,5,6},{7,8,9}};
    
    size_t size = sizeof(arr) / sizeof(arr[0]);
    
    rotateMatrix(arr, size);

    

    return 0;
}
